
services:
  c2server:
    volumes:
      - "${SSL_CERTIFICATE}:/certs/cert.pem:ro"
      - "${SSL_CERTIFICATE_KEY}:/certs/key.pem:ro"
    environment:
      C2_SERVER_SSL_CERT: "/certs/cert.pem"
      C2_SERVER_SSL_KEY: "/certs/key.pem"
